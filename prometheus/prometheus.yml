global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "property-backend"
    metrics_path: "/api/metrics"
    static_configs:
      # Scrape the backend service directly on the shared Docker network.
      - targets: ["backend:8000"]
